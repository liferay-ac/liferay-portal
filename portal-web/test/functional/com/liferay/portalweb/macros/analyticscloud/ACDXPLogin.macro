definition {

	macro logout {
		ACUtils.clearLocalStorage();

		User.logoutPG();
	}

	macro logoutAndLogin {
		ACUtils.clearLocalStorage();

		User.logoutAndLoginPG(userLoginEmailAddress = "${userLoginEmailAddress}");
	}

}