components:
    schemas:
        AssetHistogramMetric:
            properties:
                histograms:
                    items:
                        $ref: "#/components/schemas/Histogram"
                    type: array
        AssetMetric:
            properties:
                assetId:
                    type: string
                assetTitle:
                    type: string
                assetType:
                    type: string
                dataSourceId:
                    type: string
                defaultMetric:
                    $ref: "#/components/schemas/Metric"
                selectedMetrics:
                    items:
                        $ref: "#/components/schemas/Metric"
                    type: array
        AssetPageHistogramMetric:
            properties:
                pageHistograms:
                    items:
                        $ref: "#/components/schemas/PageHistogram"
                    type: array
        Histogram:
            properties:
                metricName:
                    type: string
                metrics:
                    items:
                        $ref: "#/components/schemas/Metric"
                    type: array
                total:
                    format: double
                    type: number
        Metric:
            properties:
                metricType:
                    type: string
                previousValue:
                    format: double
                    type: number
                previousValueKey:
                    type: string
                trend:
                    $ref: "#/components/schemas/Trend"
                value:
                    format: double
                    type: number
                valueKey:
                    type: string
        PageHistogram:
            properties:
                assetHistogramMetrics:
                    items:
                        $ref: "#/components/schemas/AssetHistogramMetric"
                    type: array
                pageTitle:
                    type: string
        Trend:
            properties:
                percentage:
                    format: double
                    type: number
                trendClassification:
                    enum: [NEGATIVE, NEUTRAL, POSITIVE]
                    type: string
info:
    license:
        name: "Apache 2.0"
        url: "http://www.apache.org/licenses/LICENSE-2.0.html"
    title: ""
    version: v1.0
openapi: 3.0.1
paths:
    "{groupId}/asset-metrics/{assetType}":
        get:
            operationId: getGroupAssetMetric
            parameters:
                - in: path
                  name: groupId
                  schema:
                      format: int64
                      type: integer
                - in: path
                  name: assetType
                  schema:
                      type: string
                - in: query
                  name: assetId
                  schema:
                      type: string
                - in: query
                  name: identityType
                  schema:
                      enum: [ALL, KNOWN, UNKNOWN]
                      type: string
                - in: query
                  name: rangeKey
                  schema:
                      type: integer
                - in: query
                  name: selectedMetrics
                  schema:
                      items:
                          type: string
                      type: array
            responses:
                200:
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/AssetMetric"
                        application/xml:
                            schema:
                                $ref: "#/components/schemas/AssetMetric"
            tags: ["AssetMetric"]
    "{groupId}/asset-metrics/{assetType}/histogram":
        get:
            operationId: getGroupAssetMetricHistogram
            parameters:
                - in: path
                  name: groupId
                  schema:
                      format: int64
                      type: integer
                - in: path
                  name: assetType
                  schema:
                      type: string
                - in: query
                  name: assetId
                  schema:
                      type: string
                - in: query
                  name: identityType
                  schema:
                      enum: [ALL, KNOWN, UNKNOWN]
                      type: string
                - in: query
                  name: rangeKey
                  schema:
                      type: integer
            responses:
                200:
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/AssetHistogramMetric"
                        application/xml:
                            schema:
                                $ref: "#/components/schemas/AssetHistogramMetric"
            tags: ["AssetMetric"]
    "{groupId}/asset-metrics/{assetType}/pages/histogram":
        get:
            operationId: getGroupAssetMetricPageHistogram
            parameters:
                - in: path
                  name: groupId
                  schema:
                      format: int64
                      type: integer
                - in: path
                  name: assetType
                  schema:
                      type: string
                - in: query
                  name: assetId
                  schema:
                      type: string
                - in: query
                  name: identityType
                  schema:
                      enum: [ALL, KNOWN, UNKNOWN]
                      type: string
                - in: query
                  name: rangeKey
                  schema:
                      type: integer
            responses:
                200:
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/AssetPageHistogramMetric"
                        application/xml:
                            schema:
                                $ref: "#/components/schemas/AssetPageHistogramMetric"
            tags: ["AssetMetric"]